class ProductComponent {
  imageUrl: string = "/assets/product.jpg";

  loadProduct(): void {
    this.bindImageSource("imageUrl"); // ERROR: Passing literal string instead of property
    // Should be: this.bindImageSource(this.imageUrl);
  }

  private bindImageSource(src: string): void {
    // Method expects property value, not literal string
  }